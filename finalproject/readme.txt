Go to build folder and run lab10.sln
Set lab10 as the start project.
Go to the debug tab in the top left and click start debugging.

Basically exact same process as all previous projects.

Controls:

Camera:
-WASD to move and mouse to look around

Car:
-Arrow keys to drive the car.

Quit
-ESC to exit the program.